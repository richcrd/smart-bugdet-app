import { View, Text } from "react-native";

export default function TabsIndex() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Pantalla temporal</Text>
    </View>
  );
}
